/** Card-like surface for a standalone data table (rounded-xl, bg-card, soft shadow) — the same
 * look as the Orders / Products / Expenses tables. */
export const TABLE_SURFACE_CLASS = "overflow-hidden rounded-xl border border-border bg-card shadow-[var(--shadow-soft)]"

/** Muted, compact header row for any data table. Put it on the `TableHeader`. */
export const TABLE_HEADER_CLASS = "bg-muted/40"

/** Header cell text: small, muted, sentence case. */
export const TABLE_HEAD_CLASS = "px-4 text-xs font-medium text-muted-foreground"
